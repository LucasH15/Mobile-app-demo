import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    taskItem: {
        flexDirection: "row",
        justifyContent: "space-between",
        alignItems: "center"
    },
    status: {
        paddingVertical: 5,
        paddingHorizontal: 3,
        height: "auto"
    }
});
